// Copy this file to config.js and fill in your Google Maps API key.
// Google Maps: https://console.cloud.google.com/google/maps-apis/credentials
//   Enable: Maps JavaScript API + Places API (New)
//   Restrict the key to your domain under "HTTP referrers" in Google Cloud Console.
//
// GEMINI KEY — do NOT add it here.
//   For local dev:  add GEMINI_API_KEY to a .env.local file and run `vercel dev`.
//   For production: add GEMINI_API_KEY in your Vercel project's Environment Variables UI.
//   The key is kept server-side via api/gemini.js and never reaches the browser.

window.GOOGLE_MAPS_API_KEY = 'YOUR_GOOGLE_MAPS_API_KEY_HERE';
